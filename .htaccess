Options -MultiViews
RewriteEngine On
RewriteBase /

# deja pasar archivos/carpetas reales
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# *** CLAVE *** no reescribir nada de /api/
RewriteRule ^api/ - [L]

# fallback SPA
RewriteRule ^ index.html [L]
